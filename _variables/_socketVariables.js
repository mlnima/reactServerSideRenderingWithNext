// import io from 'socket.io-client';
// export const wSocket = process.env.NEXT_PUBLIC_SOCKET === 'true' ?  io(process.env.NEXT_PUBLIC_PRODUCTION_URL.replace(process.env.REACT_APP_PORT, parseInt(process.env.REACT_APP_PORT) + 1)) : null;

// webSocket.on('connect',cb=>{

// })



export const socketOnConnect = (socket) =>{

}