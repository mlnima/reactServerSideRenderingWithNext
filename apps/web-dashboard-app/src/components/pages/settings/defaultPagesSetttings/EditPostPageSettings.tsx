import React, { FC } from 'react';
import styled from 'styled-components';

const Style = styled.form``;

interface PropTypes {}

const EditPostPageSettings: FC<PropTypes> = ({}) => {
    return (
        <Style>
            <h3>Edit Post fields:</h3>
        </Style>
    );
};
export default EditPostPageSettings;
