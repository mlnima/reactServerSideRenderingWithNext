import {FC} from "react";
import styled from "styled-components";

const Style = styled.div``;

interface PropTypes {
    AttendersIds:string[]
}

const Attenders: FC<PropTypes> = ({AttendersIds}) => {
    return (
        <Style>

        </Style>
    )
};
export default Attenders;