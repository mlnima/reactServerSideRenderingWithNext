import React, { FC } from 'react';
import UserPageActionButtons from '../components/UserPageActionButtons/UserPageActionButtons';
import AuthorPostsNavigation from './components/AuthorPostsNavigation';

interface IProps {
}

const page: FC<IProps> = ({}) => {
  return (
    <div>

posts
    </div>
  );
};

export default page
