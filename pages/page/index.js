import React, { useEffect, useState, useContext, useRef } from 'react';

const page = props => {
    const [ state, setState ] = useState({});
    useEffect(() => {
    }, []);
    return (
        <div>page</div>
    );
};
export default page;
