{
  "name": "api-requests",
  "version": "1.0.0",
  "private": true,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean:bash": "rm -rf dist && rm -rf .turbo && rm -rf node_modules",
    "clean:cmd": "rmdir /s /q dist && rmdir /s /q .turbo && rmdir /s /q node_modules",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\"",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "eslint": "^8.36.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^18.16.14",
    "jest": "^29.4.1",
    "jest-presets": "*",
    "custom-util": "*",
    "eslint-config-custom": "*",
    "tsconfig": "*",
    "typescript": "latest"
  }
}