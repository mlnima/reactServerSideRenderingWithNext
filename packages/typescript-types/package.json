{
  "name": "typescript-types",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "scripts": {
    "build": "echo 'No build necessary for types package'",
    "dev": "echo 'No build necessary for types package'",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\"",
    "test": "jest"
  },
  "files": [
    "dist/**"
  ],
  "dependencies": {
    "typescript": "^5.4.5"
  }
}