import React, {useEffect, useState, useContext, useRef} from 'react';

const CardElementBottomInfo = props => {
    const [state, setState] = useState({});
    useEffect(() => {
    }, []);
    return (
        <div>

        </div>
    );
};
export default CardElementBottomInfo;
